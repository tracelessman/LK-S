const { Router } = require('express')

const router = Router()
const util = require('../util')
const config = require('../../config')
const {dbCode} = config

// router.get('/deleteAllMsg')

module.exports = router
